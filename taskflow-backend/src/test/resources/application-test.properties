# ─── Test Profile ───────────────────────────────────────────────────────────
# Flapdoodle auto-configures the embedded MongoDB URI; no mongo URI needed here.
spring.data.mongodb.auto-index-creation=true
de.flapdoodle.mongodb.embedded.version=7.0.4

# JWT config (must satisfy @Value injection in JwtUtil)
jwt.secret=TestSecretKeyForJWTTokenGenerationInTestsSuperSecure2024
jwt.expiration=86400000
