spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskflow_dev?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: TaskFlowDevSecretKey2024
  expiration: 86400000
